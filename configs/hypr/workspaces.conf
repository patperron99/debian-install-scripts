# Workspace Rules
# on-created-empty: launch an app when the workspace is first opened empty
# Customize to match your workflow.

# workspace = 1, on-created-empty:slack
# workspace = 9, on-created-empty:brave

# Window → workspace assignment (block syntax)
# windowrule {
#     name = slack-to-ws1
#     match:class = ^(Slack)$
#     workspace = 1
# }
